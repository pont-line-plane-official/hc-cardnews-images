# hc-cardnews-images

하트커넥트 인스타그램 카드뉴스 이미지 호스팅용 저장소입니다.

- 인스타그램 게시 API(Instagram Content Publishing)가 **Meta 서버에서 직접 받아갈 수 있는 공개 URL**만 받기 때문에 존재합니다.
- 생성은 `heart-connect-docs`(비공개)의 `marketing/content_engine` 에서 하고, 이 저장소에는 결과 PNG만 올라옵니다.
- 경로 규칙: `<slug>/4x5/NN.png`
